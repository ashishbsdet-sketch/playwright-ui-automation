{
  "name": "playwright-ui-automation",
  "version": "1.0.0",
  "description": "Production-style Playwright and TypeScript UI automation portfolio",
  "private": true,
  "scripts": {
    "test": "playwright test --project=chromium",
    "test:all": "playwright test",
    "test:headed": "playwright test --headed --project=chromium",
    "test:debug": "playwright test --debug --project=chromium",
    "test:typecheck": "tsc --noEmit",
    "report": "playwright show-report",
    "report:enhanced": "node scripts/generate-enhanced-report.js",
    "mcp:start": "node mcp-server/index.js",
    "mcp:start:heal": "MCP_ALLOW_HEAL=true npm run mcp:start"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  },
  "dependencies": {
    "dotenv": "^17.4.2"
  }
}
